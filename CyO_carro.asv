disp("===== CONTROLABILIDAD DE CARRO =====");

W_c = ctrb(A, B_c(:,1));
rango_W_c = rank(W_c);
n = size(A,B_c(:,1));

if rango_W_c == n
    disp("El sistema es controlable desde T_tm")
else
    disp()